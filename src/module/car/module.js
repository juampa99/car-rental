/**
* @param {import('express').Application} app
* @param {import('rsdi').IDIContainer} container
* */
function init(app, container){

}

module.exports = {
    init,
    /* Resto del modulo, requiere implementacion */
    /* Rest of the module, requires implementation */
}
